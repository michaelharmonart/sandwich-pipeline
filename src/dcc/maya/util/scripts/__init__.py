"""Standalone Maya menu scripts (FX_Card_Rig, Camera, ALT_Camera, ...).

Loaded by MEL shelf entries via `from dcc.maya.util.scripts.<name> import X`
(or, through the Phase 4 compatibility shim, `from dcc.maya.util.scripts.<name>
import X` — Phase 5 rewrites the shelves to the canonical path).
"""

from __future__ import annotations
